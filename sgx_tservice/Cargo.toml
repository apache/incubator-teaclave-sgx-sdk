[package]
name = "sgx_tservice"
version = "0.9.7"
authors = ["Baidu"]

[lib]
name = "sgx_tservice"
crate-type = ["rlib"]

[features]
default = []

[dependencies]
sgx_types = { path = "../sgx_types" }
